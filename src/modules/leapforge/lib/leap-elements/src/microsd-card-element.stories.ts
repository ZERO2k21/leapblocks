import { html } from 'lit';
import './microsd-card-element';

export default {
  title: 'microSD Card',
  component: 'leap-microsd-card',
};

const Template = () => html`<leap-microsd-card></leap-microsd-card>`;

export const Default = Template.bind({});
