import { html } from 'lit';
import './tilt-switch-element';

export default {
  title: 'Tilt Switch',
  component: 'leap-tilt-switch',
};

const Template = () => html` <leap-tilt-switch></leap-tilt-switch>`;

export const Default = Template.bind({});
