services:
  - type: web
    name: electra-compile-server
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 3001
    healthCheckPath: /health
    autoDeploy: true
